{
  "name": "seamless.ts",
  "module": "src/server/index.ts",
  "type": "module",
  "devDependencies": {
    "bun-types": "latest"
  },
  "peerDependencies": {
    "typescript": "^5.0.0"
  },
  "scripts": {
    "dev": "bun run --hot src/server/index.ts",
    "build": "NODE_ENV=production bun build --compile src/server/index.ts --outfile dist/server"
  }
}